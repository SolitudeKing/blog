# 当前进度

更新时间：2026-07-16。

## 可验证状态

| 领域 | 状态 | 当前事实 | 下一验收 |
| --- | --- | --- | --- |
| 公开站点 | 进行中 | 首页、文章、归档、搜索、About、RSS 与 sitemap 已有实现 | 真实浏览器、窄屏、空状态和读屏复验 |
| 管理后台 | 进行中 | 登录、文章、专题、标签、媒体、公告和站点配置已有管理链路 | 完整表单错误、键盘流程和真实数据冒烟 |
| 专题模型 | 已落地 | 固定三专题契约已写入前后端；现有数据库迁移与旧表映射已完成实启核对 | 增加隔离数据库自动化迁移与重复启动回归 |
| 数据与缓存 | 进行中 | MySQL 为事实来源；公开文章和站点配置已有 Redis 缓存 | 缓存失效、数据库故障与并发写入验证 |
| 部署 | 待环境验收 | Compose 仅打包 api + nginx；外部 MySQL / Redis 由托管方提供，备份责任随之转移 | 目标机器展开、启动、重启与外部依赖连通性验收 |
| 自动化测试 | 不完整 | Go 测试与前端类型/构建检查可运行 | 前端单元、组件和 E2E 测试 |
| 可访问性 | 待验收 | 设计规范已定义焦点、键盘、对比度与动效降级 | 真实浏览器和读屏基线 |

## 本轮完成

- 运行基线收敛为当前 `web`、`server`、`deploy`，并保留 `docs` 与静态设计蓝图 `demo`。
- 清除旧迁移脚手架、无调用的异步任务占位代码和相关过时文档；`demo` 不参与运行但继续作为设计依据。
- 固定三个专题：雾里拾笺 `NODES/nodes`、微光造物 `CODE/code`、风过留痕 `JOTTING/jotting`。
- 移除示例文章与默认示例标签，并保留当前数据库升级所需兼容逻辑。
- 已按稳定 slug 与引用关系清理当前数据库中的历史测试文章、测试专题和无引用测试标签，并复核站点配置保持不变。
- 收紧 Compose 凭据与端口，缩小 Docker 构建上下文，补充 Nginx 基础安全配置。
- 切到外部 MySQL / Redis 托管：Compose 删除自带 `mysql` / `redis` 服务与备份脚本；新增 `MYSQL_TLS` / `REDIS_TLS` 字段支持跨网络传输加密。
- 重写 README、文档索引、维护指南和项目审查记录。

## 近期任务

1. 在目标部署机完成 Compose 与备份恢复验收。
2. 完成 Refresh Token 服务端撤销链路。
3. 加固媒体上传与删除引用安全。
4. 建立前端自动化、浏览器截图与读屏验收基线。

详细风险和优先级见 [项目审查](../../04-operations/reviews/01-project-review.md) 与 [实施路线图](../../02-requirements/roadmap/01-implementation-roadmap.md)。
