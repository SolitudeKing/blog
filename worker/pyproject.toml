[project]
name = "solitude-blog-worker"
version = "0.1.0"
description = "Celery worker for Solitude Blog"
requires-python = ">=3.12"
dependencies = [
  "celery[redis]>=5.4.0",
  "pydantic>=2.10.0",
  "pydantic-settings>=2.7.0",
]

[tool.ruff]
line-length = 100

